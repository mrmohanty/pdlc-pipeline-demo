version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - echo "Installing dependencies"
  build:
    commands:
      - echo "Build step - no zip now"

artifacts:
  files:
    - '**/*'
